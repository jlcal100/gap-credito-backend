[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma db push --skip-generate || true; node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
