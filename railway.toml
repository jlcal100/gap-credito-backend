[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "echo 'PORT='$PORT && echo 'DB='$DATABASE_URL | head -c 30 && echo '...' && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
