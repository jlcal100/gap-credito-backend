[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma db push || true; node prisma/seed.js || true; node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
