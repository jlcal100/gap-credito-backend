[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma db push && node prisma/seed.js && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
